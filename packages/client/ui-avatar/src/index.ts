/**
 * Avatar overlay plugin, Host half. The browser half is discovered through
 * the package manifest's dsh.client declaration.
 */

/** Host plugin body; Avatar presentation and persistence stay in the browser. */
export function apply(): void {}
