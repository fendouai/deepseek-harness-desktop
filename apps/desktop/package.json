{
  "name": "dsh-desktop",
  "description": "Tauri desktop host for the bundled dsh runtime and existing Web UI",
  "version": "0.1.0-rc.5",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:runtime": "node scripts/prepare-runtime.mjs",
    "dev": "pnpm run prepare:runtime && tauri dev",
    "build": "pnpm run prepare:runtime && tauri build",
    "test": "pnpm run prepare:runtime && cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
